<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.wasalny">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".Activities.SecurityActivity"></activity>
        <activity android:name=".Activities.Client.PreviousJourneyActivity" />
        <activity android:name=".Activities.ChatActivity" />
        <activity android:name=".Activities.RoomsActivity" />
        <activity android:name=".Activities.Driver.DisplayJourneyActivity" />
        <activity android:name=".Activities.Client.ClientProfileActivity" />
        <activity android:name=".Activities.Client.AddJourneyActivity" />
        <activity android:name=".Activities.Driver.HomeDriverActivity" />
        <activity android:name=".Activities.Driver.DriverRegister" />
        <activity android:name=".Activities.Client.ClientRegister" />
        <activity android:name=".Activities.Client.HomeClientActivity" />
        <activity android:name=".Activities.RegisterActivity" />
        <activity android:name=".Activities.LoginActivity" />
        <activity android:name=".Activities.AuthActivity" />
        <activity
            android:name=".Activities.MainActivity"
            android:theme="@style/FullScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>